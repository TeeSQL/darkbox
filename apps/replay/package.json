{
  "name": "@darkbox/replay",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Standalone cinematic replay of a finished DarkBox game — markets, daemons, TVL and billboard, rendered as a scrubbable video.",
  "scripts": {
    "gen": "tsx scripts/gen.ts",
    "predev": "tsx scripts/gen.ts",
    "dev": "vite",
    "prebuild": "tsx scripts/gen.ts",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}
