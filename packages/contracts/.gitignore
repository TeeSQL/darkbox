# Foundry
out/
cache/
broadcast/

# forge-std is fetched via the contracts setup script and not vendored.
lib/forge-std/

# NOTE: lib/frontier/ is intentionally NOT ignored — it is the vendored real
# Frontier orderbook source (see lib/frontier/PROVENANCE.md) that the DarkBox
# prediction-market integration deploys and tests against.
