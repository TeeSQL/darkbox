{
  "name": "@darkbox/bridge",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "fund:daemons": "node --import tsx scripts/fund-daemons.ts"
  },
  "dependencies": {
    "@darkbox/shared": "workspace:*",
    "viem": "^2.21.0",
    "zod": "^4.2.1"
  }
}
